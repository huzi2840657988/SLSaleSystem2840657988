用户菜单列表显示：

建立信息分页支持类（工具）:
制作分页及分页显示的跳转按钮：
	1.传入的常量：0.1 总条数；
			    0.2 当前页；
			    0.3 页面容量；
			    0.4 总页数；
			    0.5 页面列表集合
			    0.6 按钮前后需要显示的跳转按钮数
	2.封装；
	3.在对应拿到的总条数下计算总页数；
	4.是否存在当前一页，下一页，及按钮数量	



1.	设置前台传入的参数并绑定；
2.	获取当前的session中存放的基础数据模型信息到Map数组中；
3.	数据模型判断
		01.数据模型空----跳转至主界面
		02.否则：
		   01. 获取roleList列表（接口，map.xml等，调用方法）；
		   02.新建用户实例，将对应信息（字符串不为空，数值StringUtils.isNullOrEmpty）调用SQL处理后放入user；
				01.创建分页支持对象；
				02.查询需要显示的用户信息总数及当前页信息放入分页支持实例；
				03.计算页面显示信息及起始页码信息放入user对象，根据此查询userList；
					01.StarNum=((page.getPage() - 1) * page.getPageSize());
					02.PageSize(page.getPageSize());
				04.将用户分页信息放入page对象，否则置空；
		03.page,传入参数的信息，rolist，放入model,
		04.页面跳转至list；











